package com.infiniteVision.schoolProject.modules.scholarship.enums;

/**
 * Lifecycle status for a student scholarship application (discount approval only).
 */
public enum ScholarshipApplicationStatus {

    PENDING,
    PRINCIPAL_APPROVED,
    APPROVED,
    REJECTED
}
